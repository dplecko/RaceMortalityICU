
ricu:::init_proj()
root <- rprojroot::find_root(rprojroot::has_file(".gitignore"))
set.seed(2024)

#' * Heterogeneity of (z, w)-DE_{x_0, x_1} (re-admission)* 
src <- "anzics"

if () {
  
  
} else {
  
  dat <- load_data(src)
}
